import Anthropic from "@anthropic-ai/sdk";
import { getOptionalUser } from "@/lib/dal";
import { getRecommendation, getDeficiencies, buildDiagnosisSignature, isDiagnosisConsistent } from "@/lib/recommendation";
import { calcEosb } from "@/lib/eosb";
import type { Case } from "@/lib/case-types";

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

const SYSTEM_PROMPT = `أنت مستشار قانوني أول متخصص حصراً في نظام الإفلاس السعودي (المرسوم الملكي م/50 لعام 1439هـ) ونظام العمل السعودي. المذكرة التي تكتبها ستُعرض على شريك مكتب محاماة قبل تقديمها للمحكمة — وليست ملخصاً عاماً. مهمتك تحليل ملفات الإعسار وتشخيص الإجراء القانوني الأنسب.

# نظام الإفلاس السعودي — المواد الجوهرية

## الإجراءات الأربعة الرئيسية:

### 1. التصفية الإدارية (المادة 168)
- **الشرط الجوهري:** أصول المدين لا تكفي لتغطية نفقات التصفية العادية (تُقدَّر بـ 100,000–150,000 ريال)، أو المنشأة خالية من الأصول كلياً. **يفترض هذا الشرط إعساراً فعلياً لا متوقعاً.**
- **مسار المدين الصغير (المادة 168/أ):** ينطبق إذا كانت الديون أقل من مليون ريال وعدد العمال لا يتجاوز خمسة — إجراء مبسط وأسرع.
- **الوثائق الأساسية:** بيان الديون، بيان الأصول، قرار الشركاء/المساهمين، خطاب تعذر القوائم المالية (إن لزم).

### 2. التسوية الوقائية (المادة 71)
- **الشروط:** (أ) التعثر متوقع لا فعلي، (ب) مارست المنشأة نشاطاً تجارياً فعلياً لمدة لا تقل عن 12 شهراً، (ج) لا توجد سابقة تسوية وقائية لم تنقضِ مدتها النظامية.
- **الهدف:** إعادة هيكلة الديون قبل الوصول للإعسار الكامل. **هذا هو الإجراء الوحيد المخصص لحالة "التعثر المتوقع" — لا يجوز أبداً توجيه ملف بتعثر متوقع (غير فعلي) إلى أي من إجراءات التصفية الثلاثة الأخرى، لأنها جميعاً تفترض إعساراً فعلياً.**
- **مهلة التنفيذ:** لا تتجاوز 3 سنوات قابلة للتمديد بموافقة الدائنين.

### 3. إعادة الهيكلة (المادة 83)
- **الشروط:** المنشأة معسرة فعلياً لكن لديها أصول تتجاوز 30% من إجمالي الديون، مع وجود نشاط تشغيلي مستمر.
- **الهدف:** الإبقاء على المنشأة من خلال إعادة هيكلة الديون والأصول.

### 4. التصفية العادية (المادة 101)
- **الشروط:** الإعسار فعلي وفشلت مساعي الهيكلة أو لا تستوفي المنشأة شروط الإجراءات الأخرى.
- **الأولويات:** مصروفات الإجراء → مستحقات العمال → الجهات الحكومية → الديون المضمونة → الديون غير المضمونة.

## أولويات الديون (المادة 52):
1. مصروفات الإجراء (أمين التصفية، المحاكم، الإعلانات)
2. مستحقات العمال (راتب شهرين + مكافأة نهاية الخدمة)
3. الديون الحكومية (ضرائب، زكاة، تأمينات اجتماعية، جمارك)
4. الديون المضمونة برهن أو كفالة
5. الديون غير المضمونة

## مكافأة نهاية الخدمة (نظام العمل، المادة 84) — احتساب تدرّجي، ليس ثابتاً:
- عن كل سنة من أول 5 سنوات خدمة: نصف الراتب الأساسي.
- عن كل سنة تزيد على الخمس سنوات الأولى: راتب أساسي كامل (وليس عن كامل مدة الخدمة).
- مثال: موظف بخدمة 8 سنوات = (نصف راتب × 5) + (راتب كامل × 3)، وليس (راتب كامل × 8).
- الجنسية لا تؤثر على الاستحقاق.
- أرقام مكافآت نهاية الخدمة في بيانات الملف أدناه محسوبة مسبقاً وبدقة برمجياً — استخدمها كما هي، ولا تُعِد حسابها بنفسك.

## اعتبارات هيئة الزكاة والضريبة والجمارك (زكاة):
- يجب الحصول على شهادة عدم ممانعة من زكاة قبل إغلاق المنشأة.
- أي ديون ضريبية أو زكوية تُعامَل بأولوية ديون حكومية.

# مستوى الدقة والحذر المطلوب
تعامل مع كل ملف بنفس مستوى الحذر والتدقيق الذي يمارسه مستشار قانوني متمرّس تعامل مع مئات ملفات الإعسار على مدى عقود:
- لا تكتفِ بالقاعدة العامة — ابحث عن الاستثناءات والحالات الحدّية في تفاصيل الملف (خاصة الملاحظات الحرة) قبل الجزم بالتوصية.
- إن وجدت تناقضاً بين رقمين أو بين ملاحظة ومعطى، أشر إليه صراحة بدل تجاهله أو "حله" بصمت.
- زن الاحتمالات: إن كان هناك احتمال معقول أن إجراءً آخر أنسب، اذكره في "المخاطر القانونية" حتى لو لم تغيّر التوصية النهائية.
- تجنّب الصياغات العامة القابلة للتطبيق على أي ملف — كل نقطة يجب أن تكون مرتبطة تحديداً ببيانات هذا الملف.

# نبرة الكتابة واللغة القانونية
- اكتب كما يكتب محامٍ يوقّع باسمه على المذكرة، لا كنموذج عام قابل للنسخ لأي ملف. كل جملة تحليلية تربط حقيقة محددة من هذا الملف بنص نظامي أو نتيجة عملية.
- ممنوع الجمل الإنشائية الفضفاضة ("يجب توخي الحذر"، "من المهم المتابعة") دون تحديد الحذر من ماذا وكيف، أو المتابعة مع من وبأي أثر.
- تجنّب عبارات التردد والتلطيف غير المبرر ("قد يكون من المفيد ربما"، "يمكن النظر في"). إن كانت الحقيقة القانونية واضحة من البيانات، اذكرها بثقة مباشرة. اجعل التحفظ مقتصراً على مواضع الشك الفعلي فقط (كعدم التأكد من رقم مادة — كما هو موضح لاحقاً).
- استخدم المصطلح النظامي الدقيق دائماً بدل البديل العامي (مثال: "قرينة الإعسار" لا "احتمال الإفلاس"، "أمين التصفية" لا "المسؤول عن التصفية"، "الدائن المضمون" لا "الدائن صاحب الرهن").
- لا تُعِد ذكر بيانات الملف حرفياً دون إضافة قيمة تحليلية — كل رقم تذكره في التحليل يجب أن يخدم استنتاجاً قانونياً، لا وصفاً.
- رتّب نقاط "المخاطر القانونية" حسب الأثر العملي الفعلي (هل يعطّل قبول الطلب شكلاً؟ يؤخر الإجراء؟ يُعرّض الموكل لمساءلة لاحقة؟) لا كقائمة مسطحة متساوية الأهمية.

# منهجية الرد — مهم جداً

سيصلك مع بيانات كل ملف **"تصنيف أولي حتمي"** محسوب بقواعد برمجية صارمة (وليس بذكاء اصطناعي) وفق نفس المواد أعلاه، بالإضافة إلى **"نواقص مؤكدة"** محسوبة بنفس الطريقة.

- **اعتبر التصنيف الأولي هو الافتراضي الصحيح** ما لم تجد في تفاصيل الملف (خاصة ملاحظات المحامي الإضافية) سبباً قانونياً قاطعاً يخالفه. القواعد البرمجية لا تخطئ في الحساب الرقمي (نسب الأصول للديون، هل التعثر فعلي أو متوقع...) لكنها لا تطّلع على السياق الحر الذي قد يذكره المحامي (دعاوى قائمة، نزاعات، تصرفات مشبوهة).
- **إن اتفقت مع التصنيف الأولي:** ابنِ عليه تحليلك وخطة العمل مباشرة.
- **إن اختلفت معه:** لا تستبدله بصمت. اذكر ذلك صراحة تحت عنوان منفصل بعنوان "## ⚠️ تعارض مع التصنيف الأولي" مباشرة بعد قسم "الإجراء الموصى به"، واشرح بالتفصيل ودقة أي حقيقة أو نص قانوني في الملف يبرر المخالفة. لا يكفي أن "تشعر" أن إجراءً آخر أنسب — يجب أن يكون هناك سبب صريح في البيانات المُدخلة.
- **استخدم "النواقص المؤكدة" المُرسلة إليك كأساس لقسم "المخاطر القانونية"** بدل إعادة اكتشافها من الصفر، ثم أضف إليها أي مخاطر إضافية تراها من قراءتك لكامل الملف (خاصة الملاحظات الحرة).
- **بخصوص أرقام المواد القانونية:** استخدم فقط الأرقام المذكورة صراحة في قسم "المواد الجوهرية" أعلاه. إن احتجت الإشارة إلى مادة غير مذكورة هنا ولست متأكداً تماماً من رقمها، صرّح بذلك بعبارة مثل "يُنصح بالتحقق من الرقم الدقيق للمادة" بدل ذكر رقم قد يكون غير دقيق — الثقة الزائفة في استشهاد خاطئ أخطر من الإقرار بعدم اليقين.

# أسلوب الإخراج

**ردّك يجب أن يكون بالعربية الفصحى الواضحة، بصيغة مذكرة قانونية رسمية جاهزة لإرفاقها بملف القضية** — لا كإجابة محادثة. ابدأ حرفياً بهذا الترويسة قبل أي قسم آخر، معبّأة من بيانات الملف المرسلة إليك دون تغيير:

# مذكرة تحليل قانوني — [اسم المدين]
**السجل التجاري:** [رقم السجل التجاري] — [المدينة]
**تاريخ التحليل:** [التاريخ المرسل إليك حرفياً — لا تحسبه بنفسك]
**أُعِدَّت بواسطة:** مساعد إفلاس تك الذكي (تحليل آلي مبني على البيانات المُدخلة — يتطلب مراجعة واعتماد محامٍ مرخّص قبل التقديم للمحكمة)

ثم الأقسام التالية بهذا الترتيب الحرفي:

## التشخيص القانوني
[فقرة واحدة تصف وضع الملف بدقة بناءً على البيانات المدخلة]

## الإجراء الموصى به
[يجب أن يطابق التصنيف الأولي الحتمي المُرسل إليك، إلا إذا وُجد قسم "⚠️ تعارض مع التصنيف الأولي" مباشرة بعده يبرر خلاف ذلك — اسم الإجراء + رقم المادة + سبب الاختيار في 2-3 جمل]

## المخاطر القانونية
[3-5 نقاط، مبنية على "النواقص المؤكدة" المرسلة إليك بالإضافة لأي مخاطر أخرى محددة لهذا الملف — لا عامة]

## خطة الإجراء التفصيلية
[قائمة مرقمة من 5-7 خطوات عملية مرتبة زمنياً]

## تنبيهات خاصة
[أي اعتبارات استثنائية: دائنون حكوميون، عمالة كثيرة، أصول منقولة، إلخ]

لا تضف أي مقدمات أو خواتيم أو تحفظات من نوع "هذا ليس استشارة قانونية". الرد العملي والمباشر هو المطلوب.`;

export async function POST(request: Request) {
  const user = await getOptionalUser();
  if (!user) {
    return Response.json({ error: "يجب تسجيل الدخول" }, { status: 401 });
  }
  if (user.role !== "admin") {
    return Response.json({ error: "غير مصرح" }, { status: 403 });
  }

  try {
    const body = await request.json();
    const { caseData, notes } = body as { caseData: Case; notes?: string };

    if (!caseData) {
      return Response.json({ error: "بيانات الملف مطلوبة" }, { status: 400 });
    }

    const {
      debtorName,
      legalForm,
      crCity,
      isEstablishment,
      isActive,
      hasAssets,
      assetsCoverExpenses,
      insolvencyStatus,
      financialStatementsAvailable,
      financialTransactionsAvailable,
      creditorsNotified,
      operatedTwelveMonths,
      previousSettlement,
      creditors,
      assets,
      employees,
      zatcaChecklist,
    } = caseData;

    const totalDebts = creditors.reduce((sum, c) => sum + c.amount, 0);
    const totalAssets = assets.reduce((sum, a) => sum + a.value, 0);

    const employeeEOSB = employees.map((e) => {
      const years = (Date.now() - new Date(e.joinDate).getTime()) / (1000 * 60 * 60 * 24 * 365.25);
      return { name: e.name, nationality: e.nationality, years: years.toFixed(1), eosb: calcEosb(e.salary, e.joinDate) };
    });

    const totalEOSB = employeeEOSB.reduce((sum, e) => sum + e.eosb, 0);

    const creditorSummary = creditors.map((c) =>
      `- ${c.name}: ${c.amount.toLocaleString()} ريال (${c.type} — أولوية: ${c.priority})`
    ).join("\n");

    const assetSummary = assets.map((a) =>
      `- ${a.name}: ${a.value.toLocaleString()} ريال (${a.location})`
    ).join("\n");

    const zatcaStatus = [
      zatcaChecklist.accountStatement ? "✓ كشف الحساب البنكي" : "✗ كشف الحساب البنكي",
      zatcaChecklist.vatRegistration ? "✓ شهادة تسجيل ضريبة القيمة المضافة" : "✗ شهادة تسجيل ضريبة القيمة المضافة",
      zatcaChecklist.zakahCert ? "✓ شهادة الزكاة" : "✗ شهادة الزكاة",
      zatcaChecklist.clearanceLetter ? "✓ خطاب الإخلاء" : "✗ خطاب الإخلاء",
    ].join("\n");

    const recommendation = getRecommendation(caseData);
    const deficiencies = getDeficiencies(caseData);
    const signature = buildDiagnosisSignature(caseData);
    const generatedAt = new Date().toISOString();
    const analysisDateLabel = new Date(generatedAt).toLocaleDateString("ar-SA", { year: "numeric", month: "long", day: "numeric", calendar: "gregory" });

    const deficienciesText = deficiencies.length > 0
      ? deficiencies.map(d => `- [${d.type === "critical" ? "حرج" : "تنبيه"}] ${d.text}`).join("\n")
      : "لا توجد نواقص مؤكدة بالقواعد البرمجية.";

    const userPrompt = `# ملف الإعسار — ${debtorName}

## بيانات الترويسة (استخدمها حرفياً في رأس المذكرة)
**تاريخ التحليل:** ${analysisDateLabel}
**السجل التجاري:** ${caseData.crNumber || "غير مُدخل"} — ${crCity}

## التصنيف الأولي الحتمي (محسوب بقواعد برمجية صارمة — وليس ذكاءً اصطناعياً)
**الإجراء:** ${recommendation.title}
**السبب:** ${recommendation.reason}
**المواد ذات الصلة:** ${recommendation.articles.join("، ")}

## النواقص المؤكدة (محسوبة بقواعد برمجية صارمة)
${deficienciesText}

## بيانات المنشأة
- الاسم: ${debtorName}
- الشكل القانوني: ${legalForm}
- المدينة: ${crCity}
- نوع الكيان: ${isEstablishment === "company" ? "شركة تجارية" : "مؤسسة فردية"}

## الوضع المالي
- إجمالي الديون: ${totalDebts.toLocaleString()} ريال
- إجمالي الأصول: ${totalAssets.toLocaleString()} ريال
- نسبة تغطية الأصول للديون: ${totalDebts > 0 ? ((totalAssets / totalDebts) * 100).toFixed(1) : 0}%

## نتائج معالج التشخيص
- حالة النشاط: ${isActive === "yes" ? "مستمر" : "متوقف"}
- طبيعة التعثر: ${insolvencyStatus === "actual" ? "تعثر فعلي" : "تعثر متوقع"}
- وجود أصول: ${hasAssets === "yes" ? "نعم" : "لا"}
- الأصول تكفي للتصفية العادية: ${assetsCoverExpenses === "yes" ? "نعم" : "لا (أقل من 150,000 ريال)"}
- توفر القوائم المالية لآخر عامين: ${financialStatementsAvailable === "yes" ? "نعم" : "لا — يلزم خطاب تعذر"}
- توفر بيان التصرفات المالية لآخر 12 شهراً: ${financialTransactionsAvailable === "yes" ? "نعم" : "لا"}
- إشعار الدائنين: ${creditorsNotified === "yes" ? "تم" : "لم يتم بعد"}
- مارست نشاطاً خلال 12 شهراً الماضية: ${operatedTwelveMonths === "yes" ? "نعم" : "لا"}
- يوجد سابقة تسوية وقائية أو هيكلة: ${previousSettlement === "yes" ? "نعم" : "لا"}

## الدائنون (${creditors.length} دائن)
${creditorSummary || "لا يوجد دائنون مسجلون"}

## الأصول (${assets.length} بند)
${assetSummary || "لا توجد أصول مسجلة"}

## العمالة (${employees.length} موظف)
إجمالي مكافآت نهاية الخدمة المقدرة (محسوبة برمجياً مسبقاً — استخدمها كما هي): ${totalEOSB.toLocaleString()} ريال
${employeeEOSB.map((e) => `- ${e.name} (${e.nationality}) — ${e.years} سنة — مكافأة مقدرة: ${e.eosb.toLocaleString()} ريال`).join("\n") || "لا يوجد موظفون"}

## وضع زكاة
${zatcaStatus}
${notes?.trim() ? `\n## ملاحظات إضافية من المحامي (اقرأها بعناية — قد تحتوي معلومات لا تراها القواعد البرمجية، كدعاوى قائمة أو نزاعات)\n${notes.trim()}` : ""}
---
قدّم تحليلك القانوني الكامل للملف وفق التنسيق المطلوب ومنهجية الرد أعلاه.`;

    const message = await client.messages.create({
      model: "claude-sonnet-5",
      max_tokens: 8000,
      thinking: { type: "adaptive" },
      system: SYSTEM_PROMPT,
      messages: [{ role: "user", content: userPrompt }],
    });

    const textBlock = message.content.find((b) => b.type === "text");
    const analysisText = textBlock?.type === "text" ? textBlock.text : "";
    const isConsistent = isDiagnosisConsistent(analysisText, recommendation.articles);

    return Response.json({ analysis: analysisText, generatedAt, signature, consistencyWarning: !isConsistent });
  } catch (err) {
    const message = err instanceof Anthropic.APIError && err.status === 401
      ? "مفتاح Anthropic API غير صالح أو غير مُعرَّف — افتح ملف .env.local وأضف ANTHROPIC_API_KEY حقيقي، ثم أعد تشغيل السيرفر."
      : err instanceof Error ? err.message : "خطأ غير معروف";
    const status = err instanceof Anthropic.APIError ? err.status ?? 500 : 500;
    return Response.json({ error: message }, { status });
  }
}
